<section class="cta">
    <div class="container cta__inner">
        <!-- Top label -->
        <span class="cta__label label" data-reveal>Start a Project</span>

        <!-- Headline -->
        <h2 class="cta__headline" data-reveal data-delay="0.1">
            Ready to build something
            <span class="cta__headline-em">remarkable?</span>
        </h2>

        <!-- Sub -->
        <p class="cta__sub" data-reveal data-delay="0.2">
            Let's talk about your next project. We'd love to hear what you're
            working on.
        </p>

        <!-- Actions -->
        <div class="cta__actions" data-reveal data-delay="0.3">
            <a href="/contact" class="btn btn--dark cta__btn-primary">
                Get in Touch
                <svg
                    width="18"
                    height="18"
                    viewBox="0 0 18 18"
                    fill="none"
                    aria-hidden="true"
                >
                    <path
                        d="M3.75 9h10.5M9.75 4.5l4.5 4.5-4.5 4.5"
                        stroke="currentColor"
                        stroke-width="1.5"
                        stroke-linecap="round"
                        stroke-linejoin="round"></path>
                </svg>
            </a>
            <a href="mailto:hello@northframe.studio" class="cta__email">
                hello@northframe.studio
            </a>
        </div>

        <!-- Decorative grid lines -->
        <div class="cta__grid-lines" aria-hidden="true">
            <span></span><span></span><span></span><span></span>
        </div>
    </div>
</section>

<style>
    .cta {
        background: var(--clr-accent);
        position: relative;
        overflow: hidden;
        padding-block: clamp(5rem, 12vw, 9rem);
    }

    /* ─── Inner layout ───────────────────────────────────────── */
    .cta__inner {
        position: relative;
        z-index: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        gap: 1.75rem;
    }

    /* ─── Label ──────────────────────────────────────────────── */
    .cta__label {
        color: rgba(0, 0, 0, 0.5);
    }

    .cta__label::before {
        background: rgba(0, 0, 0, 0.35);
    }

    /* ─── Headline ───────────────────────────────────────────── */
    .cta__headline {
        font-size: clamp(2.8rem, 7vw, 7rem);
        font-weight: 400;
        color: var(--clr-bg);
        letter-spacing: -0.03em;
        line-height: 1;
        max-width: 14ch;
    }

    .cta__headline-em {
        display: block;
        font-style: italic;
        color: rgba(0, 0, 0, 0.45);
    }

    /* ─── Sub ────────────────────────────────────────────────── */
    .cta__sub {
        font-size: clamp(1rem, 1.5vw, 1.15rem);
        color: rgba(0, 0, 0, 0.55);
        max-width: 44ch;
        line-height: 1.7;
        font-weight: 400;
    }

    /* ─── Actions ────────────────────────────────────────────── */
    .cta__actions {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: center;
        gap: 1.25rem;
        margin-top: 0.5rem;
    }

    .cta__btn-primary {
        background: var(--clr-bg);
        color: var(--clr-text);
        border: none;
        font-size: 0.95rem;
        padding: 0.9rem 2rem;
    }

    .cta__btn-primary:hover {
        background: var(--clr-surface-2);
        transform: translateY(-2px);
    }

    .cta__email {
        font-family: var(--font-mono);
        font-size: 0.85rem;
        letter-spacing: 0.02em;
        color: rgba(0, 0, 0, 0.6);
        padding-bottom: 2px;
        border-bottom: 1px solid rgba(0, 0, 0, 0.25);
        transition:
            color var(--dur),
            border-color var(--dur);
    }

    .cta__email:hover {
        color: var(--clr-bg);
        border-color: var(--clr-bg);
    }

    /* ─── Decorative grid lines ──────────────────────────────── */
    .cta__grid-lines {
        position: absolute;
        inset: 0;
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        pointer-events: none;
        opacity: 0.06;
    }

    .cta__grid-lines span {
        border-right: 1px solid var(--clr-bg);
        height: 100%;
    }
    .cta__grid-lines span:first-child {
        border-left: 1px solid var(--clr-bg);
    }
</style>
